using System;

namespace KabitaLabwork
{
    internal class Q10
    {
        /*static void Main()
        {
            // Input character
            Console.WriteLine("Enter an alphabet: ");
            char ch = Convert.ToChar(Console.ReadLine().ToLower());

            // Using switch statement to check for vowel or consonant
            switch (ch)
            {
                case 'a':
                case 'e':
                case 'i':
                case 'o':
                case 'u':
                    Console.WriteLine(ch + " is a vowel.");
                    break;
                default:
                    if (char.IsLetter(ch))
                    {
                        Console.WriteLine(ch + " is a consonant.");
                    }
                    else
                    {
                        Console.WriteLine(ch + " is not a valid alphabet.");
                    }
                    break;
            }
        }*/
    }
}
